<div class="index-header">
	<h2>
		<%= @nutrient.name %>
		<%= link_to "Edit", edit_nutrient_path(@nutrient), class: "btn btn-primary pull-right" %>
	</h2>
</div>

<% @nutrient.benefits.each do |b| %>

	<%= b.name %>

<% end %>
