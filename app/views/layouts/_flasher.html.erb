$(document).ready(function(){
	<% if flash[:error] %>
		$.pnotify({
	    text: '<%= flash[:error] %>',
	    type: 'error',
	    history: false,
	    icon: false,
	    sticker: false,
	    closer: true,
	    styling: 'fontawesome'
		});
	<% elsif flash[:notice] %>
		$.pnotify({
	    text: '<%= flash[:notice] %>',
	    type: 'success',
	    history: false,
	    icon: false,
			sticker: false,
	    closer: true,
	    styling: 'fontawesome'
		});
	<% elsif flash[:alert] %>
		$.pnotify({
	    text: '<%= flash[:alert] %>',
	    type: 'notice',
	    history: false,
	    icon: false,
	    sticker: false,
	    closer: true,
	    styling: 'fontawesome'
		});
	<% end %>
});