<div class="row">
	<div class="col-md-9">
		<div class="well" id="post-form">
			<%= form_for @post do |f| %>
				<%= render 'layouts/errors', object: @post if @post.errors.any? %>
				<!--
				<div class="row">
					<div class="col-md-12">
						<%= render 'tags/tag_list', taggable: @post %>
					</div>
				</div>
				-->
				<div class="row">
					<div class="col-md-12">
						<%= f.label :name %>
						<%= f.text_field :name, class: "form-control" %>
					</div>
					<!--
					<div class="col-md-6">
						<%= render 'tags/tag_form', taggable: @post %>
					</div>
					-->
				</div>
				<br>
				<%= f.label :headline %>
				<%= f.text_area :headline, class: "form-control", rows: 2 %>
				<br>
				<%= f.label :abstract %>
				<%= f.text_area :abstract, class: "form-control", rows: 3 %>
				<br>
				<%= f.cktext_area :body %>
				<br>
				<%= link_to "Cancel", :back, class: "btn btn-default" %>
				<%= f.submit "Save", class: "btn btn-primary", data: {loading_text: "Saving..."} %>
				&nbsp;
				<%= f.check_box :core_content %>
				<%= f.label :core_content, "Core Content?", style: "margin-top: 6px; margin-left: 5px; position: absolute" %>
			<% end %>
		</div>
	</div>
	<div class="col-md-3 text-center">
		<%= render 'images/imageable', resource: @post %>
	</div>
</div>
<br>
