<div class="index-header">
	<h2>
		<%= @post.title %>
		<%= link_to "Edit Post", edit_post_path(@post), class: "btn btn-primary pull-right" if can? :manage, @post %>
	</h2>
</div>
<%= @post.body.html_safe %>
