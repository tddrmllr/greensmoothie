<div class="row">
	<div class="col-md-8">
		<div class="well" id="post">
			<%= image_tag @post.image.image.url(:original), style: "width: 100%" if @post.image? %>
			<br>
			<br>
			<%= @post.body.html_safe %>
			<%= show_tags(@post) %>
		</div>
	</div>
	<div class="col-md-4">
		<%= link_to "Edit Post", edit_post_path(@post), class: "btn btn-primary btn-lg btn-block", style: "margin-bottom: 10px" if can? :manage, @post %>
		<%= render 'layouts/sidebar' %>
	</div>
</div>
<%= render 'layouts/social', sociable: @post %>
<div class="row">
	<div class="col-md-6">
		<%= render 'comments/commentable', commentable: @post %>
	</div>
</div>
